# Instructions for new release

`bash get.sh`
Copy `/SDL/include` –> `/include`.

#### Mac:

Open `/SDL/XCode/SDL/SDL.xcodeproj`. Select 'Static Library' scheme. Set to release ('Edit Scheme'). Compile. Open built target in finder and copy to folder.
